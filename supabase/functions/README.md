# Supabase Functions

Direct account invitations require a server-side Supabase Auth Admin function. The browser dashboard intentionally does not contain or receive a secret/service-role key. Until that function is deployed, conference administrators can manage existing user roles, school assignments, status, and password-reset emails from the dashboard.
