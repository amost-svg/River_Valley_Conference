# Legacy application code

The retired Flask prototype was removed during the Replit migration. Its history
remains available through Git if an old implementation detail ever needs to be
reviewed. Cloudflare Pages does not build or execute anything in this directory.

The Express server remains at the repository root temporarily as a reference while
its API endpoints are migrated to Supabase. It is not part of the default build.
