# Public build-time settings for the React app.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-key

# Temporary only: the Cloudflare Pages Function can proxy legacy /api traffic
# while those endpoints are moved to Supabase. Do not commit the real value.
API_ORIGIN=https://your-temporary-api-origin.example.com
